name: ai-service CI

on:
  push:
    branches: [main]
    paths:
      - "services/ai-service/**"
      - "shared/**"
      - ".github/workflows/ai-service.yml"
  pull_request:
    paths:
      - "services/ai-service/**"
      - "shared/**"
      - ".github/workflows/ai-service.yml"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore services/ai-service/AiService.Api/AiService.Api.csproj

      - name: Build
        run: dotnet build services/ai-service/AiService.Api/AiService.Api.csproj --no-restore -c Release

      - name: Test
        run: dotnet test services/ai-service/AiService.Tests/AiService.Tests.csproj -c Release

  docker:
    needs: build-test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v5
        with:
          context: .
          file: services/ai-service/AiService.Api/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}/ai-service:${{ github.sha }},ghcr.io/${{ github.repository }}/ai-service:latest
