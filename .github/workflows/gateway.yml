name: gateway CI

on:
  push:
    branches: [main]
    paths:
      - "services/gateway/**"
      - "shared/**"
      - ".github/workflows/gateway.yml"
  pull_request:
    paths:
      - "services/gateway/**"
      - "shared/**"
      - ".github/workflows/gateway.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore services/gateway/Gateway/Gateway.csproj

      - name: Build
        run: dotnet build services/gateway/Gateway/Gateway.csproj --no-restore -c Release

  docker:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v5
        with:
          context: .
          file: services/gateway/Gateway/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}/gateway:${{ github.sha }},ghcr.io/${{ github.repository }}/gateway:latest
